language: ruby
rvm:
  - 2.4
cache: bundler
script: ./scripts/build.sh
deploy:
  provider: pages
  skip_cleanup: true
  github-token: "$GITHUB_TOKEN"
  keep-history: true
  local_dir: build
  on: turtle-master
  fqdn: hinata-104.shadow-clone-jutsu.com
  verbose: true

before_install:
  - git clone https://github.com/turtlecoin/turtlecoin-docs.git turtlecoin-docs

env:
  global:
    
